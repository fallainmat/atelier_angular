import { ContainerDirective } from './container.directive';

describe('RoundedContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerDirective();
    expect(directive).toBeTruthy();
  });
});
