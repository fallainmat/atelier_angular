import { OddsColorPipe } from './odds-color.pipe';

describe('OddsColorPipe', () => {
  it('create an instance', () => {
    const pipe = new OddsColorPipe();
    expect(pipe).toBeTruthy();
  });
});
